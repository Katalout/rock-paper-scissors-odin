# rock-paper-scissors-odin
javascript practice

1) Create function: getComputerChoice.
    - to return randomly either "rock", "paper" or "scissors"
        - use Math.random method (returns random number between 0 and 1)
        - use Math.floor and multiplication to get random 1,2 or 3
        - IF 1 then return rock, IF 2 then return scissors etc.
2) Create function: getHumanChoice.
    - use prompt method to get input
    - return input.
3) Combine these into a single round:
    -
...
Well I can't be arsed to do this, or the comments, but i might do it later. MIGHTT.

